"""
Topic: File Handling
Goal: comfortable reading from and writing to files, and using `with` as
the standard, safe way to do it.

Note: this file will create a real file called notes.txt in this same
folder when you run TODO 1. That's expected.
"""

# TODO 1: Create notes.txt (in this folder) and write 3 lines to it using
# open() with mode "w" and .write() (remember "\n" for new lines).


# TODO 2: Open notes.txt again and read its full contents, then print them.


# TODO 3: Append one more line to notes.txt (mode "a") WITHOUT erasing the
# 3 lines already there. Read the file again afterward to confirm.


# TODO 4: Read notes.txt line by line and print each line prefixed with its
# line number, e.g. "1: <first line>".


# TODO 5: Redo TODO 1-2 using a `with open(...) as f:` block instead of
# manually calling open()/close(). In a comment, write one line on why
# `with` is safer.
