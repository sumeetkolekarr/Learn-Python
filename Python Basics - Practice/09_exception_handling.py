"""
Topic: Exception Handling
Goal: comfortable anticipating and handling errors with try/except instead
of letting the program crash.
"""

# TODO 1: Write code that divides two numbers where the second number is 0,
# wrapped in try/except, catching ZeroDivisionError and printing a friendly
# message instead of crashing.


# TODO 2: Use input() to ask the user for a number. Try converting it to
# int inside a try/except, catching ValueError for when they type letters
# instead (test it by actually typing letters when you run this).


# TODO 3: Write a function safe_divide(a, b) that returns the division
# result normally, but returns None AND prints a message if b is 0 (no
# crash either way). Test it with a valid case and a b=0 case.


# TODO 4: Use try / except / else / finally together: try converting the
# string "42" to int. In `else`, print that it succeeded. In `finally`,
# print "Done checking" (this should print no matter what).


# TODO 5: Deliberately access index 10 of a 3-item list, inside a
# try/except that catches IndexError and prints a message instead of
# crashing.
